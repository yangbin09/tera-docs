---
title: 让 Claude Code 操作浏览器：自动生成新闻观察报告
sidebarTitle: 05 Claude Code 操作浏览器
---

# 让 Claude Code 操作浏览器：自动生成新闻观察报告

## 一、课程目标

这一节课要完成一个非常直观的浏览器自动化案例：

**让 Claude Code 配合 Playwright CLI，打开腾讯新闻首页，模拟真实用户浏览页面，然后自动整理新闻标题，生成一份本地新闻观察报告。**

这不是复杂爬虫课，也不是数据采集课。

这一节的重点是让学员理解：

- Claude Code 可以帮助我们创建自动化任务；
    
- Playwright CLI 可以打开浏览器、录制操作、运行任务、调试过程；
    
- 浏览器自动化可以把“人工浏览网页”变成“可重复执行的本地任务”；
    
- 最终结果可以保存成本地 Markdown 报告，方便查看和复盘。
    

---

## 二、最终效果

完成后，运行一个命令，就可以看到：

1. 浏览器自动打开；
    
2. 自动进入腾讯新闻首页；
    
3. 页面自动滚动；
    
4. 自动整理页面上可见的新闻标题；
    
5. 自动生成本地报告；
    
6. 保存一张浏览器截图。
    

最终本地会生成类似这些文件：

|文件|作用|
|---|---|
|reports/tencent-news-report.md|新闻观察报告|
|data/tencent-news-latest.json|新闻标题数据|
|data/tencent-news-latest.csv|表格数据|
|screenshots/tencent-news-home.png|页面截图|

课堂上最直观的效果是：

**学生能看到浏览器真的自己打开、自己滚动，最后本地多出一份报告。**

---

## 三、这个案例适合什么人？

适合以下学员：

- 想学习 Claude Code 的人；
    
- 想学习浏览器自动化的人；
    
- 想知道 AI 如何操作网页的人；
    
- 想做个人效率工具的人；
    
- 想做自动化测试、网页检查、信息整理的人。
    

不适合一开始就讲太深的内容，比如：

- 反爬；
    
- 代理；
    
- 并发采集；
    
- 数据库存储；
    
- 自动登录；
    
- 验证码绕过；
    
- 大规模抓取。
    

这一节课只讲一个简单闭环：

**打开网页 → 浏览页面 → 读取标题 → 保存报告。**

---

## 四、工具分工

在正式安装之前，先让学员明白每个工具负责什么。

|工具|作用|
|---|---|
|Claude Code|听懂需求，帮我们创建任务、修改配置、解释报错|
|Node.js|提供 JavaScript 运行环境|
|Playwright|真正操作浏览器|
|Playwright CLI|提供命令行能力，比如录制、运行、调试、查看报告|
|浏览器|打开网页、滚动页面、展示内容|
|本地报告|保存整理后的结果|

可以用一句话解释：

**Claude Code 是大脑，Playwright 是手，浏览器是现场，本地报告是成果。**

---

## 五、安装前准备

正式开始前，需要准备三个东西：

1. 一台电脑；
    
2. 一个终端；
    
3. 一个 Claude Code 可用账号。
    

Windows 用户可以使用：

- PowerShell；
    
- Windows Terminal；
    
- Cursor / VS Code 自带终端。
    

macOS 用户可以使用：

- Terminal；
    
- iTerm2；
    
- VS Code 自带终端。
    

---

# 第一部分：安装 Claude Code

## 1. 检查是否已经安装 Claude Code

打开终端，输入：

`claude --version`

如果能看到版本号，说明已经安装成功。

如果提示找不到命令，就需要安装 Claude Code。

---

## 2. 安装 Claude Code

Windows 用户可以在 PowerShell 中使用官方安装方式。

安装完成后，再次检查：

`claude --version`

只要能看到版本号，就说明安装成功。

---

## 3. 启动 Claude Code

创建一个练习文件夹，例如：

`browser-news-demo`

进入这个文件夹，然后输入：

`claude`

如果终端进入 Claude Code 对话界面，就说明 Claude Code 可以正常使用。

课堂讲解时可以这样说：

**以后我们不是直接手写所有配置，而是把需求告诉 Claude Code，让它帮我们搭建这个自动化案例。**

---

# 第二部分：安装 Node.js

## 1. 检查 Node.js

在终端输入：

`node -v`

再输入：

`npm -v`

如果两个命令都能看到版本号，说明 Node.js 和 npm 已经安装。

---

## 2. 如果没有安装 Node.js

直接去 Node.js 官网安装长期支持版，也就是 LTS 版本。

安装完成后，重新打开终端，再检查：

`node -v`

`npm -v`

只要能看到版本号，就可以继续。

---

# 第三部分：创建练习项目

## 1. 创建项目文件夹

在你习惯的位置创建一个新文件夹。

建议名字叫：

`browser-news-demo`

这个文件夹就是本节课的练习项目。

---

## 2. 进入项目文件夹

在终端进入这个目录。

进入后，后面的所有操作都在这个文件夹中完成。

---

## 3. 初始化项目

在终端输入：

`npm init -y`

这个命令会创建一个基础项目文件。

你不用给学员解释太多，只需要说：

**这一步是给当前文件夹创建一个 Node.js 项目身份。**

---

# 第四部分：安装 Playwright CLI

## 1. 使用官方初始化方式

在项目目录中输入：

`npm init playwright@latest`

执行后，会出现几个选择。

建议这样选择：

|问题|建议选择|
|---|---|
|使用 JavaScript 还是 TypeScript|JavaScript|
|测试文件放在哪里|tests|
|是否添加 GitHub Actions|No|
|是否安装 Playwright 浏览器|Yes|

对于初学者，不建议一开始选 TypeScript。

原因很简单：

**这节课重点是浏览器自动化，不是类型系统。**

---

## 2. 安装完成后会多出什么？

安装完成后，项目里一般会出现：

|内容|作用|
|---|---|
|tests 文件夹|放自动化任务|
|playwright 配置文件|Playwright 的基础配置|
|package.json|项目命令入口|
|node_modules|依赖文件|

课堂上不用展开讲配置文件。

只需要说：

**Playwright 已经帮我们把浏览器自动化项目的骨架搭好了。**

---

# 第五部分：认识 Playwright CLI

这节课只需要掌握 5 个命令。

## 1. codegen：录制浏览器操作

用途：

**打开浏览器，你怎么点、怎么滚动，Playwright 会帮你生成操作草稿。**

命令形式：

`npx playwright codegen 目标网址`

本案例可以打开腾讯新闻。

课堂讲解：

**codegen 就像“浏览器操作录制器”。我们先手动演示一遍，Playwright 会帮我们记录这个过程。**

---

## 2. test：运行自动化任务

用途：

**正式运行 Playwright 自动化任务。**

命令形式：

`npx playwright test`

课堂讲解：

**等 Claude Code 帮我们创建好任务后，就用 test 来执行。**

---

## 3. headed：打开真实浏览器运行

用途：

**让浏览器真的显示出来，适合课堂演示。**

命令形式：

`npx playwright test --headed`

课堂讲解：

**无界面运行虽然快，但学生看不到效果。教学时一定要用 headed 模式，让大家看到浏览器自己打开和滚动。**

---

## 4. ui：可视化调试模式

用途：

**打开 Playwright 的可视化界面，查看任务执行过程。**

命令形式：

`npx playwright test --ui`

课堂讲解：

**如果任务失败，可以用 UI 模式看它失败在哪一步。**

---

## 5. show-report：查看测试报告

用途：

**打开 Playwright 生成的测试报告。**

命令形式：

`npx playwright show-report`

课堂讲解：

**除了我们自己生成的新闻报告，Playwright 也会生成任务执行报告。**

---

# 第六部分：先用 codegen 演示一次

这一步非常适合课堂演示。

## 1. 启动录制器

在终端运行：

`npx playwright codegen https://news.qq.com/`

运行后会打开两个窗口：

1. 一个浏览器窗口；
    
2. 一个 Playwright 录制窗口。
    

---

## 2. 手动操作浏览器

你可以在浏览器里做这些动作：

1. 等腾讯新闻首页加载出来；
    
2. 向下滚动几次；
    
3. 点击一条新闻；
    
4. 返回首页；
    
5. 再滚动一下。
    

---

## 3. 给学员解释

这一步可以这样讲：

**我们刚才的手动动作，Playwright 都能记录下来。后面 Claude Code 可以把这些录制草稿整理成正式任务。**

强调一句：

**codegen 不是最终成品，它只是帮我们快速理解网页结构和操作流程。**

---

# 第七部分：让 Claude Code 创建新闻观察任务

现在进入 Claude Code。

在项目目录中输入：

`claude`

然后把下面这段提示词发给 Claude Code。

---

## 给 Claude Code 的提示词

你现在是我的浏览器自动化教学助手。

请帮我创建一个简单的 Playwright CLI 教学案例，主题是：

“让 Claude Code 操作浏览器：自动生成新闻观察报告”。

场景要求：

1. 打开腾讯新闻首页；
    
2. 使用 Playwright 操作浏览器；
    
3. 使用有界面浏览器模式，方便课堂演示；
    
4. 模拟真实用户浏览页面；
    
5. 页面加载后向下滚动几次；
    
6. 只整理页面上公开可见的新闻标题和链接；
    
7. 不登录；
    
8. 不绕过验证码；
    
9. 不采集新闻全文；
    
10. 不做高频访问；
    
11. 对标题进行简单去重；
    
12. 根据标题关键词做简单分类，例如科技、财经、社会、国际、体育、娱乐、其他；
    
13. 生成本地 Markdown 报告；
    
14. 保存一张首页截图；
    
15. 配置好方便启动的 npm 命令。
    

本地结果要求：

1. 报告保存到 reports 文件夹；
    
2. 数据保存到 data 文件夹；
    
3. 截图保存到 screenshots 文件夹；
    
4. 报告中包含采集时间、标题数量、前 20 条新闻标题、简单分类统计、截图路径。
    

启动命令要求：

1. 配置一个有界面运行命令；
    
2. 配置一个 UI 调试命令；
    
3. 配置一个查看报告命令；
    
4. 配置一个 codegen 录制命令。
    

最后请告诉我：

1. 新增了哪些文件；
    
2. 修改了哪些文件；
    
3. 每个命令怎么用；
    
4. 推荐课堂上先运行哪个命令；
    
5. 如果页面结构变化，应该怎么修复。
    

---

# 第八部分：Claude Code 执行时怎么选择

Claude Code 可能会询问你是否允许创建文件、修改配置、安装依赖。

教学时建议这样处理：

|Claude Code 行为|建议|
|---|---|
|创建 tests 文件|允许|
|创建 reports 文件夹|允许|
|创建 data 文件夹|允许|
|创建 screenshots 文件夹|允许|
|修改 package.json|允许|
|安装 Playwright|如果前面已安装，可以拒绝；如果没装，可以允许|
|访问外部网页|本案例需要访问腾讯新闻，可以允许|

注意：

**不要让 Claude Code 做登录、绕过验证码、批量抓取、频繁访问。**

如果 Claude Code 写得太复杂，可以补一句：

**请简化，不要做复杂爬虫，只保留课堂演示需要的功能。**

---

# 第九部分：启动运行

Claude Code 配置好后，项目里一般会多出几个 npm 命令。

命令名字可能略有不同，但建议统一成下面这些。

---

## 1. 有界面运行

推荐课堂第一个演示这个：

`npm run news:headed`

这个命令会：

1. 打开浏览器；
    
2. 进入腾讯新闻；
    
3. 滚动页面；
    
4. 整理标题；
    
5. 保存结果；
    
6. 关闭浏览器。
    

课堂上重点让学生看：

**浏览器真的被自动操作了。**

---

## 2. UI 模式运行

进阶演示用这个：

`npm run news:ui`

这个命令会打开 Playwright 的 UI 界面。

可以看到：

1. 当前有哪些任务；
    
2. 哪个任务执行成功；
    
3. 哪个步骤失败；
    
4. 执行过程回放；
    
5. 错误信息。
    

课堂上可以这样讲：

**如果普通运行失败，我们就用 UI 模式来看失败在哪里。**

---

## 3. 查看 Playwright 报告

运行：

`npm run news:report`

这个命令一般会打开 Playwright 自带测试报告。

注意区分两个报告：

|报告|说明|
|---|---|
|Playwright 测试报告|看自动化任务有没有成功|
|新闻观察报告|看整理出的新闻标题和统计结果|

---

## 4. 重新录制浏览器操作

如果腾讯新闻页面结构变了，可以运行：

`npm run news:codegen`

这会重新打开录制器。

你可以重新操作一遍页面，然后把生成的操作草稿交给 Claude Code，让它帮你修复任务。

---

# 第十部分：查看最终结果

运行完成后，主要看三个地方。

---

## 1. 新闻观察报告

位置一般是：

`reports/tencent-news-report.md`

这个文件是本节课的最终成果。

里面应该包含：

|内容|说明|
|---|---|
|生成时间|什么时候运行|
|新闻数量|本次整理了多少条|
|标题列表|前 20 条新闻标题|
|分类统计|科技、财经、社会等|
|截图路径|证明浏览器访问过页面|

---

## 2. 数据文件

位置一般是：

`data/tencent-news-latest.csv`

或者：

`data/tencent-news-latest.json`

CSV 可以用 Excel 打开。

JSON 适合后面继续做程序处理。

这一节课不用展开 JSON 和 CSV 的技术细节，只需要告诉学员：

**同一份内容，可以保存成报告，也可以保存成数据。**

---

## 3. 页面截图

位置一般是：

`screenshots/tencent-news-home.png`

这张图可以作为课堂展示材料。

它的作用是：

**证明浏览器确实打开过页面。**

---

# 第十一部分：课堂演示顺序

建议你按照这个顺序讲。

---

## 第 1 步：讲清楚目标

开场可以这样说：

**今天我们做一个 AI 新闻观察员。它不是替我们写文章，而是替我们打开浏览器，看公开新闻页面，然后把页面上能看到的标题整理成本地报告。**

---

## 第 2 步：讲工具分工

用这句话：

**Claude Code 负责理解任务，Playwright 负责操作浏览器，浏览器负责访问页面，本地报告负责保存结果。**

---

## 第 3 步：演示 codegen

运行：

`npx playwright codegen https://news.qq.com/`

让学生看到：

**我们手动操作浏览器，Playwright 会记录动作。**

---

## 第 4 步：让 Claude Code 创建任务

把提示词发给 Claude Code。

让它创建自动化任务。

---

## 第 5 步：有界面运行

运行：

`npm run news:headed`

这是全课最重要的演示环节。

因为学生能看到浏览器真的自己动。

---

## 第 6 步：查看报告

打开：

`reports/tencent-news-report.md`

告诉学生：

**自动化的价值不只是打开网页，而是把浏览结果保存下来，变成可以复用的本地资料。**

---

# 第十二部分：常见问题

## 1. 浏览器没有打开怎么办？

可能原因：

- Playwright 浏览器没安装完整；
    
- 项目初始化不完整；
    
- 命令不在项目目录里执行。
    

解决方式：

先确认你在项目目录里。

然后重新安装浏览器。

---

## 2. 页面打开很慢怎么办？

可能原因：

- 网络慢；
    
- 腾讯新闻页面资源较多；
    
- 浏览器等待时间太短。
    

解决方式：

让 Claude Code 帮你把等待时间调长一点。

提示词可以这样写：

**页面加载有点慢，请把等待时间调长，不要一打开就采集。**

---

## 3. 没有采集到标题怎么办？

可能原因：

- 页面结构变化；
    
- 选择器不合适；
    
- 内容还没加载完；
    
- 页面弹窗遮挡。
    

解决方式：

运行 codegen 重新观察页面。

然后告诉 Claude Code：

**页面结构可能变了，请根据当前页面重新调整标题提取方式。**

---

## 4. 运行时提示找不到命令怎么办？

可能原因：

- 没有安装依赖；
    
- 没有在项目目录里；
    
- package.json 里没有配置命令。
    

解决方式：

让 Claude Code 检查 package.json。

提示词可以这样写：

**请检查 package.json，确认 news:headed、news:ui、news:report、news:codegen 这些命令是否配置正确。**

---

## 5. 为什么不用直接爬接口？

因为这节课的目标不是讲爬虫，而是讲浏览器自动化。

浏览器自动化的价值在于：

- 能看到真实页面；
    
- 能模拟用户行为；
    
- 能截图留证；
    
- 能做页面检查；
    
- 能配合 Claude Code 排查问题。
    

---

# 第十三部分：教学边界

这节课一定要讲清楚边界。

推荐原话：

**这个案例只用于个人学习和教学演示，只整理公开页面上可见的标题和链接，不登录、不绕过验证码、不采集新闻全文、不做高频访问、不做商业化批量抓取。**

这样讲会显得更专业，也更安全。

---

# 第十四部分：这节课的核心总结

最后可以这样收尾：

**今天我们完成的不是一个复杂项目，而是一个完整闭环：从安装工具，到录制浏览器操作，再到让 Claude Code 创建自动化任务，最后生成本地新闻观察报告。**

这节课真正要学会的是：

1. Claude Code 可以帮我们搭建自动化任务；
    
2. Playwright CLI 可以录制、运行、调试浏览器操作；
    
3. 浏览器自动化可以把网页浏览变成可重复执行的流程；
    
4. 最终结果可以沉淀成本地报告；
    
5. 以后同样的方法可以迁移到博客检查、页面截图、链接检测、表单测试、后台巡检等场景。
    

---

# 第十五部分：后续升级方向

这个案例跑通后，可以继续升级。

## V1：只打开网页并截图

适合第一节演示。

## V2：读取新闻标题并保存报告

适合本节课主线。

## V3：增加关键词统计

比如统计 AI、经济、科技、体育等关键词。

## V4：增加每日对比

比较今天和昨天的热门词变化。

## V5：做成本地看板

把 Markdown 报告升级成本地网页看板。

## V6：迁移到自己的博客

让 Claude Code 检查自己的 VitePress 或个人网站：

- 页面是否能打开；
    
- 链接是否 404；
    
- 标题是否过长；
    
- 控制台是否报错；
    
- 分享卡片是否配置正确。
    

---

# 最终一句话

**让 Claude Code 操作浏览器，不是为了炫技，而是把重复的网页检查、浏览、整理工作变成自动化流程。**

这就是这节课最重要的价值。
